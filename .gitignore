node_modules
.svelte-kit
.vercel
.wrangler
.dev.vars
.env
*.log
.DS_Store
output/
wrangler.generated.toml
.claude/worktrees/
.dev/worktree/
.env*.local

# pnpm experiments — repo uses npm, package-lock.json is authoritative.
pnpm-lock.yaml
pnpm-workspace.yaml

# Secrets — defense in depth. Apple keys, GCP service accounts, etc.
*.p8
AuthKey_*.p8
*.json.key
